'use client';

// This component has been replaced by SupabaseErrorListener.
// Keeping this file for backwards compatibility - it now re-exports the Supabase version.
export { SupabaseErrorListener as FirebaseErrorListener } from './SupabaseErrorListener';
